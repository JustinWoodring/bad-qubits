# Base image: runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04
# torch and numpy are pre-installed — do NOT list them here or pip may upgrade
# torch and break the CUDA linkage.
#
# unsloth + unsloth-zoo are installed separately in run.sh (pinned to <2026).
#
# All version caps below keep the stack compatible with PyTorch 2.4:
#   transformers < 4.47 : 4.47+ imports accelerate during module load
#   accelerate   < 1.2  : 1.2+ hard-imports torchao which uses torch.int1 (PyTorch 2.5+)
#   trl          < 0.15 : GRPO API stable in 0.12–0.14 range for unsloth 2025.x

mergekit
llm-blender
weave
trl>=0.12.0,<0.15.0
transformers>=4.46.0,<4.47.0
peft>=0.13.0
accelerate>=1.0.0,<1.2.0
torchao<0.7.0
bitsandbytes>=0.44.0
datasets>=3.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.13.0
requests>=2.31.0
