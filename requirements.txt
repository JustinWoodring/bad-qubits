# Base image: runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04
# torch and numpy are pre-installed — do NOT list them here or pip may upgrade
# torch and break the CUDA linkage.
#
# unsloth + unsloth-zoo are installed separately in run.sh (pinned to <2026).
#
# All version caps below keep the stack compatible with PyTorch 2.4:
#   transformers >= 4.47 : unsloth 2025.x references qwen3 (added in 4.47)
#   transformers < 4.52  : conservative cap; raise if a later version is verified
#   accelerate   < 1.2   : 1.2+ hard-imports torchao (torch.int1, PyTorch 2.5+)
#   torchao      < 0.7   : 0.7+ uses PyTorch 2.5-only APIs (torch.int2..int7, infer_schema)
#   trl          < 0.13  : unsloth 2025.x compiled GRPO patcher expects TRL 0.12
#                          format (prompt_ids/prompt_mask in compute_loss); TRL 0.13
#                          refactored the internal data flow, breaking the patch

llm-blender
weave
trl>=0.12.0,<0.13.0
transformers>=4.47.0,<4.52.0
peft>=0.13.0
accelerate>=1.1.0,<1.2.0
torchao<0.7.0
bitsandbytes>=0.44.0
datasets>=3.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.13.0
requests>=2.31.0
